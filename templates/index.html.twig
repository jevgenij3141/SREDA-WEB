<!DOCTYPE html>
<html>
    <head>
        <title>SREDA</title>
    </head>
    <body>
        {#<h1>{{ page_title }}</h1>

        {% if user.isLoggedIn %}
            Hello {{ user.name }}!
        {% endif %}

        #}
    </body>
</html>